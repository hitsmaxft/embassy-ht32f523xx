# Simple Time Module

This is a simplified version of the time module that provides just the basic time units needed for the HAL.

The enhanced time driver (`time_driver_enhanced.rs`) should not import from this module directly. Instead, it should be standalone or import from PAC registers.

This file is created to restore a working compilation state.